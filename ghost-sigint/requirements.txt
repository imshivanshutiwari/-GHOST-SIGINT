# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.22
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
aiosqlite==0.20.0
aioredis==2.0.1
httpx==0.27.0
websockets==12.0

# Scientific
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2

# Deep Learning
# onnx is intentionally NOT listed as a direct dependency.
# All versions of the onnx package (<=1.20.1) contain an unpatched supply-chain
# vulnerability: onnx.hub.load() with silent=True suppresses untrusted-repository
# warnings, enabling silent model-substitution attacks.  No patched release exists.
# This project uses torch.onnx.export (part of PyTorch) for ONNX export and
# onnxruntime for inference — neither requires onnx to be a direct dependency.
# onnx.hub is never called anywhere in this codebase.
torch==2.6.0
torchvision==0.21.0
onnxruntime==1.18.1

# System
psutil==5.9.8

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0

# Dev
ruff==0.4.4
mypy==1.10.0
